name: default-backup-app
implements: backup-app
state:
  item_auth: contract.auth
  item_user_input: contract.user-input
  item_source: contract.filesystem
  item_destination: contract.filesystem
  access_token: str
  refresh_token: str
  source: str
  destination: str
executor: python
methods:
  setup:
    type: python
    ref: setup.py
  test:
    type: python
    ref: test.py
  schedule:
    type: python
    ref: schedule.py
  list:
    type: python
    ref: list.py
